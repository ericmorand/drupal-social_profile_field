{
  "name": "social_profile_field",
  "version": "0.1.0",
  "main": "build.js",
  "scripts": {
    "build": "node build.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "version": "npm run build && git add -A dist"
  },
  "devDependencies": {
    "ejs": "^2.5.7",
    "fs-extra": "^4.0.2",
    "node-yaml": "^3.1.1"
  }
}
